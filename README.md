# ConfigurableFormBuilder

Compose and render forms from a shared configuration. Built with React, TypeScript, and Vite. Linting and formatting use Biome.

```bash
pnpm dev
pnpm lint
pnpm format
pnpm check
```
